import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ChatScreen() {
  return (
    <ScreenPlaceholder
      title="Coach"
      description="Ask your AI coach about an exercise or your routine here. Requires a connection — everything else in PulseForge works offline."
    />
  );
}

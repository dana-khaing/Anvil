import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProgressScreen() {
  return (
    <ScreenPlaceholder
      title="Progress"
      description="Streaks, goals, and history land here — daily and monthly targets, weight-progression charts, and a streak calendar."
    />
  );
}

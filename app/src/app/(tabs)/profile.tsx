import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Profile"
      description="Your height, weight, and goal, plus account and sync settings, land here."
    />
  );
}

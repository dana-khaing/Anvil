import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function RoutinesScreen() {
  return (
    <ScreenPlaceholder
      title="Routines"
      description="Build and edit your training split here — exercise name, weight, reps, sets, and an optional video link, structured just like your notes."
    />
  );
}
